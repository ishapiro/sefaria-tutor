/**
 * Fallback translation model, used when model discovery has not run or failed.
 * Shared so the client footer and the server resolver agree on the default.
 */
export const TRANSLATION_PRIMARY_MODEL = 'gpt-5.1-chat-latest'
