# Shoresh — copy to .env and fill in values.
# For local dev with Wrangler, copy the same keys to .dev.vars (same values).
# Do not commit .env or .dev.vars.

# Server-only (used by /api/openai/chat and Nitro)
# For Nuxt runtimeConfig, you can also use NUXT_ prefix: NUXT_API_AUTH_TOKEN, NUXT_OPENAI_API_KEY
API_AUTH_TOKEN=your-auth-token-for-openai-proxy
OPENAI_API_KEY=your-openai-api-key

# Client (used by browser to call /api/openai/chat with Bearer token)
# Nuxt maps NUXT_PUBLIC_* to runtimeConfig.public
NUXT_PUBLIC_API_AUTH_TOKEN=your-auth-token-for-openai-proxy

# Session (required for auth; min 32 chars for nuxt-auth-utils)
NUXT_SESSION_PASSWORD=your-session-password-at-least-32-characters

# OAuth (for Google login)
# If redirect URL mismatch occurs in development, set the redirect URL explicitly:
# For localhost development with Wrangler (port 8787):
# NUXT_OAUTH_GOOGLE_REDIRECT_URL=http://localhost:8787/api/auth/google
# For localhost development with Nuxt dev server (port 3000):
# NUXT_OAUTH_GOOGLE_REDIRECT_URL=http://localhost:3000/api/auth/google
# Leave empty in production - it will auto-detect from the request
NUXT_OAUTH_GOOGLE_REDIRECT_URL=
NUXT_OAUTH_GOOGLE_CLIENT_ID=your-google-client-id
NUXT_OAUTH_GOOGLE_CLIENT_SECRET=your-google-client-secret

# Turnstile (CAPTCHA on registration) — get keys at https://dash.cloudflare.com/ → Turnstile
TURNSTILE_SECRET_KEY=
NUXT_PUBLIC_TURNSTILE_SITE_KEY=

# Resend (transactional email for verification, password reset, support)
RESEND_API_KEY=
